<% provide(:title, "发现") %>

<% content_for :nav do %>
    <ul class="nav navbar-nav navbar-right">
      <li id="fat-menu" class="dropdown">
        <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown">
          添加 <span class="caret"></span>
        </a>
        <ul class="dropdown-menu dropdown-menu-left" role="menu">
          <li><%= link_to "文章", new_article_url(parent_id: @content.id) %></li>
          <li class="divider"></li>
          <li><%= link_to "频道", new_channel_url(parent_id: @content.id) %></li>
        </ul>
      </li>
    </ul>
<% end %>

<div class="col-md-12">
  <% for c in @contents %>
      <div class="form-group">
            <span style="color:#105cb6;font-size:14px;">
              <%= link_to c.name, contents_url(id: c.id) %>
            </span>
            <span style="margin-left:10px;">
              <%= c.description %>
            </span>
      </div>
  <%end%>
</div>