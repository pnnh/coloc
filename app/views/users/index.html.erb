<% provide(:title, '用户列表')
   provide(:info, users_path) %>

  <%if @users.count > 0%>
  <table class="table">
    <tbody>
      <% for user in @users%>
      <tr>
	<td><%=link_to user.name, user%></td>
      </tr>
      <%end%>
    </tbody>
  </table>
  <%else%>
  没有结果
  <%end%>
